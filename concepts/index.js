// could we use git for data sync? that would be cool right?
// proto bufs?

// Think
// How to choose a port?

// read in args
let port = 3000
let connections = []

createServer()
  // .then(server => server.start(port, connections))
  // .then(server => server.createGenesis())
  // .then(server => server.pull()) // not sure this logic should be here?
  // .then(server => server.hostClient())